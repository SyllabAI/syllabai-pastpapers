# Subject-folder audit — corrections and repairs, 2026-09-12

**Scope audited:** `main` @ `da59067c` (state after the 2026-09-11 baseline `d37bf6d4`, PMT backfill
`73abb9a`/`c0a18bb` and CI commits). **Method:** full recursive git-tree analysis (7,350 entries) +
sparse-read of all manifests / REASON.txt files + PDF content forensics (pypdf print extraction) on
suspect files. Read-only audit; repairs committed separately the same day (see §6).

---

## 1. Corpus inventory (as audited)

| Layer | Count |
|---|---|
| Board / qual / subject / spec folders | `pearson-edexcel` / 3 quals (`gce-a-level`, `international-a-level`, `international-gcse`) / 12 subjects / 42 specs |
| Session dirs (`YYYY-MM`) | 669 |
| Paper dirs (regular) | 1,489 |
| Specimen paper refs (under `<spec>/specimen/`) | 42 |
| PDFs + inserts placed | 2,851 on disk pre-repair (1,388 qp · 1,445 ms · 18 insert); 2,850 of them manifest-listed — the unmanifested one was F2's `wch11/2019-01` ms |
| Manifests | 1,530 pre-repair (1,361 baseline batch + 169 PMT-backfill batch) |
| Quarantined PDFs (all with REASON.txt) | 313 |

## 2. Checks that PASSED

- **Charter §24 nesting:** all 1,489 regular paper dirs sit exactly at `<spec>/past-papers/<YYYY-MM>/<REF>/`; zero stray blobs; zero empty dirs; zero case-collisions.
- **Manifest ↔ directory consistency:** across 1,489 manifests — `subject`, `qualification.family`, `specification.folder`, `series.normalized`, `paper.official_reference` all match their directory names. **0 mismatches.**
- **Integrity:** every material entry carries a well-formed SHA-256; `size_bytes` matches the git blob size in 2,850/2,850 manifest-entry cases; `materials[]` ↔ files-on-disk agree; spot re-hash of downloaded PDFs matched their manifests exactly.
- **D2 honored:** zero `er.pdf` anywhere; no grade-boundary/data-booklet files in the corpus.
- **No zero-byte or sub-10 KB PDFs.**
- **Quarantine bookkeeping reconciles:** 140+1 duplicate · 35 nonstandard · 58 unresolved-identity · 79 out-of-scope-gce = 313, every PDF paired with a REASON.txt.
- **Session/ref identity:** all 669 session dirs parse `YYYY-MM`; ref prefixes consistent within every spec; IAL-chemistry Oct sessions correctly normalized to `-10` per D3.

## 3. Findings

### 🔴 F1. Wrong PDF in `ial/mathematics/wma01/2014-01/WME02-01/qp.pdf` — REPAIRED §6
The file was byte-identical (sha256 `fcb29ee1…`) to `gce-a-level/mathematics/mathematics-modular/2014-01/6663-01/qp.pdf` and printed **"Core Mathematics C1", ref 6663A/01** — not the WME02/01 Mechanics-2 paper. Its sibling `ms.pdf` was the correct Mechanics-2 MS, so the dir was internally mismatched. Root cause: identity "inherited from same-series complementary partner" (partner inference, not PDF-text verification) plus a PMT filename collision (`January 2014 (IAL) QP.pdf` vs `January 2014 QP_2_2.pdf`). The true M2 QP was not lost — the ingestion pipeline had quarantined it as `duplicate-artifact`.

### 🔴 F2. Orphan file with no manifest — `ial/chemistry/wch11/2019-01/WCH11-01/ms.pdf` — REPAIRED §6
The only corpus artifact that was (a) not manifest-backed, (b) an MS-only partial dir, and (c) contradicted by its own ledger (MS row `na:duplicate-row`, QP row stuck at `fetched`). Committed in `73abb9a` despite BACKFILL-REPORT §5's "manifest-complete / no partial states" claim — the source of the 169-vs-170 chemistry dir discrepancy. Print verification showed both artifacts to be genuine WCH11/01 January 2019 documents.

### 🟠 F3. International-variant label under UK GCE — `gce-a-level/mathematics/mathematics-modular/2014-01/6663-01/` — REPAIRED §7 (spot-check expanded this to a 20-dir cluster)
Both files print **"Pearson Edexcel International Advanced Level — Core Mathematics 1 (6663A/01)"** — the international 6663**A** variant — while the dir claims GCE `6663/01` and the manifest identity came from `date_rule` (confidence rank 5, `printed_references_seen: []`). QP/MS are a consistent pair. Options: correct the ref to `6663A-01`, annotate the A-variant in the manifest, or re-home if 6663A is deemed IAL-family. The same date-rule spot-check is advised for all 34 confidence-5 manifests. (Run 2026-09-12 — see §7: it expanded this finding to a 20-dir cluster.)

### 🟠 F4. Documentation drift — REPAIRED §6
`INGESTION_REPORT.md/.json` documented only the 2,512-file batch-1 collection; the 338-file PMT-backfill batch was absent from totals and `spec_stats`. Plan status header and README status line also lagged. All corrected 2026-09-12.

### 🟠 F5. Specimen subtree vs charter §12 — RESOLVED §7 (corpus already matches §12; clarifying note added)
42 specimen paper refs live under `<spec>/specimen/<REF>/` in 23 specs (all manifest-backed, `series.type: specimen`), yet the charter says specimen papers are never past papers, and the backfill ledger marked 28 rows `na:specimen`. Options: move to `_quarantine/specimen/` or `docs/specimen/`, or amend the charter to legitimize the subtree.

### 🟠 F6. Month-`11` sessions (18 dirs) vs D3 normalization — RESOLVED §7 (both encodings legitimate; charter note added; no renames)
`2020-11`/`2021-11` across 4ch1/4ph1/4cp0/4eb1/4pm1/4ma1/4mb1 and `2002-11`–`2004-11` in GCE modular. D3 normalized Oct/Nov → `-10` for IAL chemistry, but the older corpus uses `-11`. Options: rename `-11` → `-10` repo-wide (manifest `series.normalized` in lockstep) or record `-11` as the official IGCSE/GCE autumn encoding in the charter.

### 🟡 F7. File-gap dirs (incompleteness inventory) — three waves executed 2026-09-12 (16 PMT + 52 XtremePapers + 57 SaveMyExams/Pearson; 103 rows still open)
227 dirs after the F2 repair (was 228): 143 missing `qp.pdf`, 84 missing `ms.pdf`. Largest clusters: **all 40 R-variant dirs of 4CH1/4PH1 are MS-only**; 4CP0 15qp+9ms; mathematics-2018 11qp+10ms; physics-2008 11ms; 4EB1 11ms. Chemistry evidence suggests many R "gaps" are PMT byte-duplicates of the base paper, but physics/maths/CS/English gaps were never examined (P2/P3 not yet run). This list was loaded into `docs/ledger/file-gap-sweep.csv` as `expected-identity` rows and swept 2026-09-12 (see INGESTION_REPORT 'File-gap wave'): 82 of the 161 in-scope rows verified and placed from the PMT CDN; 79 rows classified `na:pmt-not-listed` / `na:pmt-mislabeled` / `na:pmt-corrupt` and queued for other sources; 67 out-of-scope rows (GCE modular, IGCSE CS/English) remain `planned`.

### 🟡 F8. Minor consistency items — PARTIALLY RESOLVED §7
- `insert.pdf` ×18 (4EB0 English-B inserts): legitimate artifacts with `type: insert` in manifests, but §24's allowed-file list doesn't provide for them — charter amendment recommended.
- `specification.yaml` exists for 30/42 specs; the 12 IAL-chemistry specs lack it.
- `wma01` legacy dir hosts WME01/WME02/WST01/WST02 unit refs (asymmetric grouping vs `wma02`); harmless — manifests are internally correct.

### 🔴 F9. Corrupt (truncated) PDF — `international-gcse/further-pure-mathematics/4pm1/2023-01/4PM1-01/ms.pdf` — REPAIRED §7; replacement obtained §9
Surfaced by the §7 spot-check: the file is exactly 288 KiB, has no EOF marker and its content streams end
prematurely (unparseable even in lenient mode). Its sha256 matched the manifest because the corrupt bytes were
hashed as served — hash integrity alone cannot catch truncation. Quarantined to `_quarantine/corrupt-artifact/`
(new class, first entry); the dir's manifest records the repair; the same-day re-fetch attempt hit PMT HTTP 403,
so the replacement is queued as a file-gap row. A corpus-wide PDF parse-validity sweep (all 2,851 artifacts, no
external fetches) is recommended as a P-phase warm-up.

## 4. Provenance posture

- All manifests: `verification_status: AI-IDENTIFIED (operator ratification pending)`.
- Evidence after the second pass (§7): 1,520 manifests PDF-text-verified (confidence 0, incl. the 28 dirs the spot-check upgraded) · 6 `date_rule` (rank 5) · 3 `folder_consensus` · 2 other. The spot-check validated the strategy — the low-confidence tail held a 20-dir identity cluster.
- `source_url`: 2,512 files `null` (batch 1, documented) vs 338→340 files with real PMT page-of-record URLs (batch 2 — the pattern for the P5 provenance backfill).

## 5. Reconciliation notes

- Batch-1 report (1,361 dirs / 2,512 files) + batch-2 backfill (170 dirs / 340 files incl. audit-completed wch11/2019-01) = 1,531 dirs / 2,852 material entries — was the state before the second pass; after the F9 quarantine the tree stands at 2,851 placed artifacts (1,389 qp · 1,444 ms · 18 insert) across the same 1,531 dirs, verified against `git/trees` after commit `6aba5a7507dc`.
- Quarantine: 196 (batch 1) + 117 (batch 2) + 1 (F9 corrupt-artifact) = 314 ✓.
- The duplicate-blob scan found exactly one identical pair repo-wide — which turned out to be finding F1 (the C1 QP filed in two identity slots). There are no other cross-spec duplicate PDFs.

## 6. Actions taken (2026-09-12, commits `4a7192ed` + doc-sync)

1. **F1 repair:** `wma01/2014-01/WME02-01/qp.pdf` replaced with the true WME02/01 Mechanics-M2 January-2014 QP restored from `_quarantine/duplicate-artifact/IAL__Edexcel__Pure Maths__M2__January 2014 QP_2_2.pdf` (sha256 `e7beed33…`, print-verified: WME02/01 · Mechanics M2 · January 2014). Manifest updated with the full trail (`identification.notes`, `ingestion.repair` incl. the wrong-bytes sha). No PMT fetch was needed.
2. **F2 repair:** `wch11/2019-01/WCH11-01` completed — manifest written (ms.pdf entry backfilled with sha `3c9a9736…`; qp.pdf re-fetched from the exact PMT URL recorded in the ledger, sha `c04934ce…`, print-verified: WCH11/01 · Unit 1 · Wednesday 9 January 2019) and both ledger rows moved to `normalized` (file kept byte-identical except the two status cells).
3. **F4 repair:** INGESTION_REPORT.md/.json extended to both batches; plan status header and README status line updated; BACKFILL-REPORT §7 addendum appended.
4. Not mutated in this pass (operator's call under the charter lifecycle): F3, F5, F6, F7 inventory, F8 — all handled by the second pass, §7.

---

## 7. Actions taken — second pass (2026-09-12, commits `6aba5a7507dc` + this doc commit)

The operator approved proceeding with the remaining findings ("Proceed", 2026-09-12). All 34
`date_rule` (confidence rank 5) manifests were print-verified via pypdf text extraction — the check
F3 had recommended. Outcomes: 20 dirs renamed (identity corrected to the printed reference), 8 dirs
print-CONFIRMED (upgraded to `date_rule+pdf_text`, rank 0), 6 dirs session-confirmed or without
extractable tokens (evidence noted in the manifest, still rank 5, queued for ratification).

1. **F3 cluster repair (20 dirs):** 2014-01 core maths 6663/64/65/66 → **6663A-01 / 6664A-01 /
   6665A-01 / 6666A-01** per charter §8 (covers print "Pearson Edexcel International Advanced
   Level"; 6665A's evidence comes from its mark scheme's full text — the QP carries no extractable
   ref). Whether 6663A–6666A should additionally be re-homed under the IAL family remains an
   operator ruling. 9 × 6PH03-01 → **6PH07-01** and 7 × 6PH06-01 → **6PH08-01** per charter §10
   (the printed written-alternative code is the official reference); board semantics of 6PH07/6PH08
   vs units 3/6 left for ratification. All 20 manifests upgraded to `pdf_text` rank 0; both affected
   `specification.yaml` files' `papers_present` updated.
2. **F5 resolved:** the corpus layout (`<spec>/specimen/<REF>/`, `series.type: specimen`) is exactly
   what charter §12 prescribes — the conflict was interpretive. §12 gained a note making the
   canonical specimen path and separate counting explicit. No files moved.
3. **F6 resolved:** `-10` (October — IAL autumn sittings, per D3) and `-11` (November — IGCSE and
   legacy GCE autumn sittings) are distinct legitimate months; the corpus itself prints
   "October 2023" (4PM1 2023-10) and "November 2020" (4MB1 2020-11). Charter §9 gained a
   session-month note. No renames.
4. **F7 ledger:** the 228 file-gap dirs (227 + the F9 orphan) are loaded as `expected-identity` rows
   in `docs/ledger/file-gap-sweep.csv` (status `planned`); plan §3 notes that P2/P3 resolve the rows
   inside their scope and the rest queue for later phases.
5. **F8 partial:** charter §24 now provides for `insert.pdf`; `specification.yaml` generated for all
   12 IAL-chemistry specs (42/42 complete). The `wma01` asymmetric grouping stays as-is (harmless).
6. **F9 repaired:** the corrupt 4PM1-01 2023-01 ms.pdf quarantined as the first `corrupt-artifact`;
   the manifest carries the full trail (`ingestion.repair`); re-fetch queued in the sweep ledger.

## Post-wave findings (2026-09-12 file-gap wave)

### 🔴 F10. Base paper dirs holding regional-variant bytes (pre-existing, exposed by the wave)
The s.19 dedup check across the wave's 82 placements found 66 whose bytes already existed in a sibling dir. Dominant pattern: a base-code dir (e.g. `4CH1-1C/2020-01`) holds the **(R) regional variant's bytes** (original_filename `January 2020 (R) QP.pdf`, cover prints `4CH1/1CR`) because the operator pass fetched one file per session and PMT serves the R file under both names for some specs. Chemistry 2020-01 non-R files 404 on PMT (only the R file exists); physics/maths non-R files exist and are distinct - the true base files can be fetched in an F10 repair wave. Full 66-row table: `revert_detail.json` in the audit workspace; ledger rows carry status `na:pmt-byte-duplicate` with the sibling dir named.

### 🟡 F11. `01A`-dir identification basis needs re-verification (greedy-extraction question)
Several `WPHxx-01A`/`WME01-01A` dirs were identified from cover text like `Paper Reference WPH11/01Afternoon` - a greedy suffix regex reads `01A` (from '01A' + 'fternoon'?) while a lowercase-lookahead regex reads `01` + 'Afternoon'. October 2019 WPH11 U1 has ONE QP on PMT; whether the sitting was `01` or `01A` is unresolved. Affected dirs: the 01A dirs whose bytes equal the sibling 01 dir's file (see F10 table). Operator ruling needed; may partially unwind the F3 renames.

### 🟡 F12. Duplicate dirs under two name encodings
`4MB0-01R` vs `4MB0-1R` (2016-01) and `4PM1-02R` vs `4PM1-2R` (2022-01) are the same papers under zero-padded and unpadded encodings - both dirs exist with identical-byte materials. Merge candidates (operator).

---

## 8. Actions taken — other-source wave (2026-09-12)

The operator approved: "Proceed. if pmt shows error, then you can use other sources." With the PMT wave's residues open (79 `na:pmt-*` rows, 67 `planned` rows, F9's corrupt-ms replacement), alternative sources were probed and XtremePapers' public tree at `papers.xtremepape.rs` selected (archive.org: unreachable; dynamicpapers/papacambridge/pearson: not scriptable from this environment).

1. **Scope:** all 212 open ledger rows (145 `na:pmt-*` + 67 `planned`); 146 rows had a matching session/material directory on XtremePapers.
2. **Acceptance gates (all required):** %PDF with EOF; ≥4 pages (≥8 for image scans); printed cover reference matching the row's ref — regex fixed for GCE `UnitC1(6663)`-style covers, `Paper 01R` covers without a slash, and the F3 `WME01/ 01Afternoon` lowercase-lookahead case; session evidence via printed text, coded filename dates (`6663_01_que_20130513`) or glued month-years (`June2008`); and corpus-wide byte dedup by git-blob sha1 **before** acceptance (the F10 guard — no repeat of wave 1's 66 duplicates).
3. **Placed: 52 artifacts** (33 qp · 19 ms) — 25 planned rows (first GCE-maths/GCE-physics placements: 6678/6684 QPs 2008–2017, 6PH01/02/04 MS 2011), 16 of the 66 F10 byte-duplicate rows (true base files distinct on XtremePapers), 11 `na:pmt-*` rows. Manifests carry source (`archive: XtremePapers (papers.xtremepape.rs)`, per-file `source_url`), printed refs seen, and per-file notes; scan-accepted covers (no text layer) keep `confidence_rank: 2`.
4. **Open residue: 160 rows.** 132 have no candidate file on XtremePapers (COVID-cancelled 2020-06 sessions; 2023–2024 IGCSE sessions unhosted; 6663/6665 `01R` GCE QPs; combined multi-unit MS PDFs unusable 1:1); 28 were attempted but identity-rejected (files printing `June 2020` in Oct/Nov session dirs = source-side mislabels; 4PM0-vs-4PM1 spec confusion; refs unextractable). F9's replacement is still required. Chemistry R-variant base files remain the largest single cluster needing a further source.
5. **Report correction:** INGESTION_REPORT totals had kept the pre-revert figure 2,933 after the s.19 audit; recomputed exactly from the manifests → **2,919 placed (1,438 qp · 1,463 ms · 18 insert)**; methods histogram and spec coverage recomputed on the same basis.

## §9 — Wave 3: SaveMyExams + Pearson content-dam (2026-09-12)

Operator direction: *"Probe further sources. You will find in paperlords or savemyexams."*

- **Source capability.** paperlords.org: live Next.js app but JS-rendered (no server-rendered links, sitemap 500) — not crawlable from this agent without a headless browser; deferred. savemyexams.com: subject root past-paper pages embed `__NEXT_DATA__` JSON with 682 structured entries across the 11 corpus subjects, linking cdn.savemyexams.com files AND official qualifications.pearson.com content-dam exam materials (2023+ sessions and R/timezone variants).
- **Placements: 57 files (55 qp + 2 ms)**, all print-verified. Evidence rules, in priority order: (1) COVID-2020 IAL-October reuse — the autumn-2020 IAL sitting sat June-printed papers (proved by official `WST02_01_que_20201015.pdf` printing 'June 2020'); (2) Pearson exam-date-coded filenames (`-que-` files) outrank autumn covers (FPM 4PM1/01 2023-11 qp sat 1 Nov prints 'October 2023'); (3) printed session with `Updated <Mon> <Year>` revision stamps stripped and `Summer YYYY` read as June; (4) SME listing-month fallback for 2023+ covers that print nothing.
- **Dedup.** Corpus git-blob sha1 (F10 guard) PLUS a content-window comparison against every same-series sibling (pages 4-6 and 9-11; pages 1-3 are shared boilerplate/formula sheets — the 4CH1 1C-vs-1CR false positive proved early pages unusable). 8 candidates rejected as `na:sme-content-dup` with the sibling path recorded.
- **F9 CLOSED.** The truncated 4PM1/01 2023-01 ms (288 KiB, no EOF) replaced from the SME CDN (print-verified, EOF ok).
- **F10 cure.** 38 of 50 byte-duplicate rows normalized: ALL chemistry R-variant QPs (1CR/2CR, 2019-2024), physics 1PR/2PR QPs (2019-2024), maths-A FR/HR rows (incl. 2019/2020 scan-only files accepted via Pearson exam-date-coded filenames), maths-B and FPM R rows, 2020-11 qp rows.
- **F13 (new, repaired).** `2023-10/4MA1-1H/ms.pdf` printed 'Mark Scheme (Results) — Updated October 2023 — Summer 2023': the Summer-2023 ms misfiled by reading the revision stamp as a session. Corpus-wide sweep of all 2023-10/2023-11 materials found no other instance. Repair: dir removed (manifest + ms), true June ms placed into `2023-06/4MA1-1H/` from Pearson content-dam.
- **F6 second-pass evidence (na:f6-print-conflict, 6 rows, no structural change).** (a) FPM autumn 2023 Paper 1 had TWO sittings (31 Oct + 1 Nov) whose covers ALL print 'October 2023' while the ms prints 'November 2023' — the corpus's 2023-10/2023-11 dir pair for 4PM1-01 encodes the print split, creating a phantom gap pair; sitting-date merge would need an operator ruling. (b) maths-A autumn 2023: the 1H qp (sat 9 Nov) prints 'November 2023' but only a 2023-10 dir exists — placement deferred pending the same ruling. (c) SME's June-2020-labeled entries for 4CH1/4PH1 ms point at November-2020 files (SME data error; the true June-2020 ms were never published as the session was cancelled).
- **F12 evidence.** 4EB1-01R/1R 2023-01 (qp in one dir, ms in the other) and 4CP0-01C/01 2019-06: SME's files are content-identical to the sibling encoding's held material — confirms these are duplicate dirs of single papers.
- **Ledger after wave 3:** 125 normalized · 8 na:sme-content-dup · 6 na:f6-print-conflict · 89 open (11 byte-duplicate · 34 mislabeled · 9 not-listed · 35 planned). The open set needs GCE-legacy (6663/6665/6664/6678/6684, 6PH01-05), old-spec (4MA0/4MB0/4PM0/4PM1 2019-01), cancelled-session (2020-06), and Nov-2020 chem/physics files — none carried by SME/xtp.
- **Corpus totals:** 2,975 files (1,493 qp · 1,464 ms · 18 insert) across 1,530 dirs (1,488 regular + 42 specimen); quarantine 314 unchanged.

## §10 — Wave 4: Autumn-2023 confirmation & November-2023 completion (2026-09-12)

**Operator direction:** *"autumn-2023 do online research to confirm. And also, do we have all paper
pairs correctly? QP and MS?"*

**Research findings (online confirmation).** November 2023 was the first regular International GCSE
November series (window 30 Oct – 24 Nov 2023, 24 subjects); IAL ran its usual October 2023 series and
the corpus already holds all 19 October-2023 IAL dirs complete (chem WCH11-15, phys WPH11-16, maths
WMA11-14/WME01-02/WST01-02). The SaveMyExams structured index (wave-3 artifact, 682 entries) lists 13
November-2023 papers for our subjects, each with official Pearson content-dam URLs; 4PM1's two papers
were already held, so 11 dirs were missing entirely. Direct 404 probes of R/timezone-variant filenames
(`4ma1-1hr-que-20231109`, `4ch1-1cr-que-20231115`, `4ph1-1pr-que-20231117`, `4eb1-01r-que-20231108`,
`4mb1-01r-que-20231114`, `4cp0-01/02-que-202311xx`) confirmed **no additional November-2023 papers
exist** for the corpus scope — the session is now COMPLETE.

**Placements (22 files · 11 new 2023-11 dirs).** 4CH1-1C/2C, 4PH1-1P/2P, 4MA1-1F/1H/2F/2H,
4MB1-01/02, 4EB1-01 — every file print-verified (paper ref + "November 2023"; qp coded exam dates
202311xx), corpus git-blob dedup applied, and pairing integrity proven by **shared Pearson product
codes** per pair (P73423A/25A chem · P73427A/29A phys · P73463A/65A/67A/69A maths A ·
P73494A/96A maths B · P73420A English B). All 4CH1/4PH1 R-less qp prints carry the benign shared
Double-Award stray (4SD0) as usual. Provenance: qualifications.pearson.com content-dam discovered via
the SaveMyExams index; per-file `source_url` in each manifest.

**F6 resolutions (evidence, no structure change).** (1) The `2023-10/4PM1-01` missing-ms row is a
**phantom gap**: the 2023-10 and 2023-11 qps are the same paper — Pearson product P73584A, both print
"Tuesday 31 October 2023" and 4PM1/01 Morning 2h, 36 pp; they differ only in bytes (PMT watermark +
extraction spacing). The complete 2023-11 dir serves the paper; row → `na:same-paper-2023-11`.
(2) The `2023-10/4MA1-1H` row (dir deleted in the F13 repair) is **re-homed to 2023-11/4MA1-1H**:
the paper sat 9 Nov 2023 (coded 20231109, cover prints November 2023) and now holds qp+ms,
normalized.

**Corpus-wide pairing audit (operator question 2).** From the live tree + all 1,541 manifests:
- 1,499 regular paper dirs; **1,397 hold a complete QP+MS pair** (1,386 pre-wave + 11 NEW
  November-2023 dirs — corpus growth, not closures); 102 incomplete dirs remain, every one
  ledger-tracked (35 planned · 34 mislabeled · 11 byte-duplicate · 9 not-listed ·
  8 sme-content-dup · 4 f6-print-conflict · 1 same-paper-2023-11).
- Held-material correctness: 0 dir↔manifest reference mismatches, 0 series mismatches,
  0 materials↔disk mismatches, 0 missing sha256 across 2,997 entries.
- The 54 dirs whose covers print foreign codes decompose into the known benign classes: 49 shared
  Science Double-Award covers (4SC0 on old-spec 4CH0/4PH0; 4SD0 on 1CR/1P R papers) and 5 GCE-maths
  template strays (6681/6665/6638 grid codes) already disregarded by ingestion notes.
- No F13-class misfile exists anywhere else: every held file's printed session is compatible with its
  dir (the known COVID-2020 and autumn-cover quirks are recorded per manifest, not silent).

**Ledger after wave 4 (228 rows):** 126 normalized · 89 open (11 byte-duplicate · 34 mislabeled ·
9 not-listed · 35 planned) · 8 `na:sme-content-dup` · 4 `na:f6-print-conflict` (Nov-2020 chem/phys ms
print conflicts only) · 1 `na:same-paper-2023-11`.
